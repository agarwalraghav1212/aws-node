pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'npm i'
            }
        }
    }
        stage('Build') {
            steps {
                dir('/var/jenkins_home/workspace/aws-node/aws-nodejs-eb') {
                    /* execute commands in the scripts directory */
        }
            }
        stage('Test') {
            steps {
                sh 'npm start'
            }
        }   
    }
}
